### Install and compile React

Install latest LTS version of node/npm from https://nodejs.org/en/

```
$ cd ./src/main/resources/website
$ npm install
$ npm run-script build
```

For production releases:

```
$ npm run-script build-prod
```